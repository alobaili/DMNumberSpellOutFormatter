    import XCTest
    @testable import DMNumberSpellOutFormatter

    final class DMNumberSpellOutFormatterTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DMNumberSpellOutFormatter().text, "Hello, World!")
        }
    }
